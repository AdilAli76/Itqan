# 🚀 خيارات النشر - اختر الأنسب لك

**التاريخ:** 2026-09-13  
**الإصدار:** v2.0.1

---

## 📊 **مقارنة الخيارات**

| الخيار | الطريقة | الوقت | السهولة | التلقائية |
|--------|--------|-------|--------|-----------|
| **الخيار 1** | GitHub Actions | ⚡ أسرع | ⭐⭐⭐⭐⭐ | 100% |
| **الخيار 2** | RDP على السيرفر | ⏱️ سريع | ⭐⭐⭐⭐ | يدوي |
| **الخيار 3** | PowerShell محلي | ⏳ وسط | ⭐⭐⭐ | نصف تلقائي |
| **الخيار 4** | نسخ يدوي | ⏳⏳ بطيء | ⭐⭐ | يدوي تماماً |

---

## 🥇 **الخيار 1: النشر التلقائي من GitHub Actions** ✅ الأفضل

### ما الذي يفعله:

```
على كل push للـ main branch:
   1️⃣ بناء التطبيق تلقائياً
   2️⃣ عمل zip للملفات
   3️⃣ نشر على التجريب تلقائياً
   4️⃣ إعادة تشغيل IIS
   5️⃣ إرسال إشعار بالنجاح
```

### المميزات:

```
✅ تلقائي 100% - لا تدخل يدوي
✅ سريع جداً - كل شيء على Github runners
✅ آمن - بدون كلمات مرور في البريد
✅ تاريخ - جميع deployments محفوظة
✅ سهل - فقط push للـ main وخلاص!
```

### الإعداد المطلوب:

```
1. إضافة بيانات الخادم في GitHub Secrets:
   Settings → Secrets and variables → Actions
   
   ثم أضف:
   • STAGING_SERVER_HOST = 65.21.213.78
   • STAGING_SERVER_USER = username
   • STAGING_SERVER_PASS = password
   
   أو استخدم SSH key بدلاً من كلمة مرور

2. الملف الجديد موجود بالفعل:
   .github/workflows/deploy-staging.yml
```

### التشغيل:

```powershell
# فقط push للـ main وسيحدث الباقي تلقائياً!
git push origin main

# أو trigger يدويّاً:
# اذهب إلى Actions → Deploy to Staging → Run workflow
```

### الوقت المتوقع:

```
⏱️ 3-5 دقائق من الـ push حتى يكون النشر مباشر
   └─ بناء: 70 ثانية
   └─ نشر: 30-60 ثانية
   └─ إعادة IIS: 10-20 ثانية
```

---

## 🥈 **الخيار 2: RDP على السيرفر** ⚡ سريع لكن يدوي

### الخطوات:

```powershell
# 1. RDP للسيرفر
mstsc /v:65.21.213.78

# 2. فتح PowerShell كـ Administrator

# 3. تحميل السكريبت من GitHub
$scriptUrl = "https://raw.githubusercontent.com/AdilAli76/Itqan/main/deploy_staging.ps1"
Invoke-WebRequest -Uri $scriptUrl -OutFile "C:\deploy_staging.ps1"

# 4. تشغيل السكريبت
C:\deploy_staging.ps1
```

### المميزات:

```
✅ سريع جداً (على السيرفر مباشرة)
✅ بدون تعقيدات شبكة
✅ مباشر وسهل
❌ يحتاج تدخل يدوي
❌ وقت أطول من الخيار 1
```

### الوقت:

```
⏱️ 2-3 دقائق فقط
```

---

## 🥉 **الخيار 3: PowerShell من جهازك** ⚠️ معقد

### الخطوات:

```powershell
cd "C:\Users\F\Downloads\kinetic_erp"

# تشغيل مع GitHub token (اختياري)
.\deploy_staging.ps1 -GitHubToken "ghp_xxxx..."
```

### المميزات:

```
✅ نصف تلقائي
❌ معقد
❌ يحتاج GitHub token
❌ قد تكون مشاكل شبكة
❌ أبطأ من الخيارات الأخرى
```

---

## 📋 **الخيار 4: نسخ يدوي** 🐌 الأبطأ

### الخطوات:

```
1. اذهب إلى GitHub Actions
2. حمّل artifact يدويّاً
3. فك الضغط
4. انسخ إلى C:\kinetic\staging-erp
5. أعد تشغيل IIS يدويّاً
```

### المميزات:

```
❌ بطيء جداً
❌ عرضة للأخطاء
❌ عمل يدوي كثير
✅ لا تحتاج تقنيات متقدمة
```

---

## 🎯 **التوصية النهائية**

```
════════════════════════════════════════════════════════
🌟 الخيار 1: GitHub Actions (الأفضل بكثير)
════════════════════════════════════════════════════════

لماذا؟
   ✅ تلقائي 100%
   ✅ سريع جداً
   ✅ آمن
   ✅ سهل الصيانة
   ✅ لا تحتاج تدخل يدوي بعد الإعداد

الإعداد:
   1. أضف GitHub Secrets (5 دقائق فقط)
   2. قم بـ push للـ main
   3. سينشر تلقائياً!

الوقت:
   ⏱️ إعداد أول مرة: 10 دقائق
   ⏱️ كل push بعدها: 5 دقائق تلقائي
```

---

## 📝 **خطوات إعداد GitHub Actions (الخيار 1)**

### 1️⃣ افتح GitHub

```
https://github.com/AdilAli76/Itqan
```

### 2️⃣ اذهب إلى Settings

```
Settings → Secrets and variables → Actions
```

### 3️⃣ أضف الـ Secrets

```
New repository secret:

Name:  STAGING_SERVER_HOST
Value: 65.21.213.78

---

Name:  STAGING_SERVER_USER
Value: [اسم المستخدم على السيرفر]

---

Name:  STAGING_SERVER_PASS
Value: [كلمة المرور]
```

### 4️⃣ اختبر الـ Workflow

```
اذهب إلى: Actions → Deploy to Staging
اضغط: Run workflow → Run workflow
```

### 5️⃣ انتظر النتائج

```
يجب أن تشوف ✅ في خلال 5 دقائق
```

---

## ❓ **السؤال**

```
أي خيار تفضل؟

[ ] 1️⃣ GitHub Actions (تلقائي كامل) - الموصى به
   
[ ] 2️⃣ RDP على السيرفر (سريع، يدوي قليل)
   
[ ] 3️⃣ PowerShell محلي (نصف تلقائي)
   
[ ] 4️⃣ نسخ يدوي (بسيط لكن بطيء)
```

**اختر رقم واحد وأساعدك في الإعداد** 👈

---

**ملاحظة:** إذا اخترت الخيار 1 (الموصى به)، يمكنك أن تخبرني:
- اسم المستخدم على السيرفر
- كلمة المرور (أو SSH key)

وأنا سأكمل الإعداد لك!
