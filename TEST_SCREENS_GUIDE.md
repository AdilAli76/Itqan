# 🧪 دليل شاشات الاختبار

## 📱 الشاشات المتاحة

تم إضافة 3 شاشات اختبار جديدة للنظام:

### 1. **اختبار LocalDatabase** `/test/local-db`
### 2. **اختبار الفواتير** `/test/invoices`  
### 3. **اختبار Sync Queue** `/test/sync-queue`

---

## 🚀 كيفية الوصول للشاشات

### **الطريقة الأولى: من القائمة الجانبية**

```
1. سجّل الدخول للنظام
2. انظر إلى القائمة الجانبية على اليسار
3. ستجد مجموعة "اختبار" جديدة 🐛
4. اضغط على أي اختبار تريده
```

### **الطريقة الثانية: من شريط البحث (لوحة الأوامر)**

```
1. اضغط Ctrl+K (أو Cmd+K على Mac)
2. ابحث عن: "LocalDatabase" أو "الفواتير" أو "Sync Queue"
3. اضغط Enter لفتح الشاشة
```

### **الطريقة الثالثة: عنوان المتصفح المباشر**

```
http://localhost:5000/app?route=/test/local-db
http://localhost:5000/app?route=/test/invoices
http://localhost:5000/app?route=/test/sync-queue
```

---

## 📊 تفاصيل كل شاشة اختبار

### **1. اختبار LocalDatabase** 🗄️

**الموقع:** `/test/local-db`

#### الأزرار المتاحة:

```
┌─────────────────────────────────────┐
│ اختبار LocalDatabase                │
├─────────────────────────────────────┤
│                                     │
│ 📋 السجل يعرض نتائج الاختبار هنا   │
│                                     │
│ [➕] [📋] [✏️] [💰] [📄] [🔄] [🗑️] │
│                                     │
└─────────────────────────────────────┘
```

#### الاختبارات المتاحة:

| الزر | الاختبار | الوصف |
|-----|---------|------|
| ➕ | إضافة منتج | إنشاء منتج جديد في البيانات المحلية |
| 📋 | جلب منتجات | جلب قائمة المنتجات المحفوظة |
| ✏️ | تحديث منتج | تعديل سعر المنتج |
| 💰 | إنشاء فاتورة | إنشاء فاتورة جديدة مع عنصر |
| 📄 | جلب الفواتير | عرض قائمة الفواتير |
| 🔄 | Sync Queue | جلب العناصر المعلقة للمزامنة |
| 🗑️ | مسح السجل | تنظيف نافذة النتائج |

#### النتيجة المتوقعة:

```
✅ LocalDatabase initialized
📊 Database Structure:
   ✓ Table: products
   ✓ Table: invoices
   ✓ Table: invoice_items
   ✓ Table: customers
   ✓ Table: sync_queue

━━━ اختبار: إضافة منتج ━━━
✅ Product inserted: prod-001
```

---

### **2. اختبار الفواتير** 💰

**الموقع:** `/test/invoices`

#### الأزرار المتاحة:

```
┌─────────────────────────────────────┐
│ اختبار الفواتير (Invoices)          │
├─────────────────────────────────────┤
│                                     │
│ 📋 السجل يعرض نتائج الاختبار هنا   │
│                                     │
│ [💰] [📦] [📋] [📄] [📊] [🔍] [🗑️] │
│                                     │
└─────────────────────────────────────┘
```

#### الاختبارات المتاحة:

| الزر | الاختبار | الوصف |
|-----|---------|------|
| 💰 | إنشاء فواتير | إنشاء 3 فواتير تجريبية |
| 📦 | إضافة عناصر | إضافة منتجات للفواتير |
| 📋 | جلب العناصر | عرض عناصر كل فاتورة |
| 📄 | جميع الفواتير | عرض قائمة الفواتير |
| 📊 | ملخص البيانات | حساب الإجماليات والضرائب |
| 🔍 | التفاصيل الكاملة | عرض فاتورة كاملة مع عناصرها |
| 🗑️ | مسح السجل | تنظيف نافذة النتائج |

#### النتيجة المتوقعة:

```
━━━ اختبار: إنشاء فواتير متعددة ━━━
✅ تم إنشاء فاتورة 1: inv-20260915-001
✅ تم إنشاء فاتورة 2: inv-20260915-002
✅ تم إنشاء فاتورة 3: inv-20260915-003

━━━ اختبار: ملخص الفواتير ━━━
📊 ملخص المبيعات:
   • عدد الفواتير: 3
   • إجمالي المبيعات: 1785 ريال
   • إجمالي الضرائب: 170 ريال
   • إجمالي الخصومات: 85 ريال
   • الصافي: 1530 ريال
```

---

### **3. اختبار Sync Queue** 🔄

**الموقع:** `/test/sync-queue`

#### الأزرار المتاحة:

```
┌─────────────────────────────────────┐
│ اختبار Sync Queue                   │
├─────────────────────────────────────┤
│                                     │
│ 📋 السجل يعرض نتائج الاختبار هنا   │
│                                     │
│ [📋] [➕] [📊] [✅] [🔄] [🗑️]      │
│                                     │
└─────────────────────────────────────┘
```

#### الاختبارات المتاحة:

| الزر | الاختبار | الوصف |
|-----|---------|------|
| 📋 | جلب المعلقات | عرض العناصر المعلقة للمزامنة |
| ➕ | إضافة للطابور | إضافة 3 عمليات (INSERT, UPDATE, DELETE) |
| 📊 | الإحصائيات | عرض إحصائيات المزامنة |
| ✅ | وضع علامة نجاح | وضع علامة نجاح على عنصر |
| 🔄 | محاكاة مزامنة | تشغيل عملية مزامنة كاملة |
| 🗑️ | مسح السجل | تنظيف نافذة النتائج |

#### النتيجة المتوقعة:

```
━━━ اختبار: إضافة عناصر لـ Sync Queue ━━━
📝 إضافة عنصر INSERT...
✅ تم إضافة عنصر INSERT
📝 إضافة عنصر UPDATE...
✅ تم إضافة عنصر UPDATE
📝 إضافة عنصر DELETE...
✅ تم إضافة عنصر DELETE

━━━ اختبار: إحصائيات المزامنة ━━━
📊 إحصائيات المزامنة:
   • العناصر المعلقة: 3
   • العمليات:
      - INSERT: 1
      - UPDATE: 1
      - DELETE: 1
   • إجمالي محاولات الإعادة: 0
```

---

## 🧪 خطوات الاختبار الموصى بها

### **اختبار شامل (5-10 دقائق)**

```
1️⃣ افتح LocalDatabase Test
   └─ اضغط: ➕ إضافة منتج
   └─ اضغط: 📋 جلب منتجات
   └─ اضغط: 🔄 Sync Queue

2️⃣ افتح Invoices Test
   └─ اضغط: 💰 إنشاء فواتير
   └─ اضغط: 📦 إضافة عناصر
   └─ اضغط: 📊 ملخص البيانات

3️⃣ افتح Sync Queue Test
   └─ اضغط: ➕ إضافة للطابور
   └─ اضغط: 🔄 محاكاة مزامنة
   └─ اضغط: 📋 جلب المعلقات (تحقق من انخفاض العدد)
```

---

## 📋 قائمة التحقق

بعد الانتهاء من الاختبارات:

```
☐ LocalDatabase Test يعمل بنجاح
☐ جميع جداول البيانات موجودة (5 جداول)
☐ Invoices Test يعمل بنجاح
☐ يمكن إنشاء فواتير وعناصر
☐ الملخصات تحسب بشكل صحيح
☐ Sync Queue Test يعمل بنجاح
☐ يمكن إضافة عناصر للطابور
☐ محاكاة المزامنة تعمل بشكل صحيح
☐ لا توجد أخطاء في Console
```

---

## 🐛 استكشاف الأخطاء

### **المشكلة: لا تظهر مجموعة "اختبار" في القائمة**

```
✓ تحقق: هل أعدت تشغيل التطبيق؟
✓ جرّب: flutter clean && flutter pub get && flutter run
```

### **المشكلة: خطأ "Screen not found"**

```
✓ تحقق: أن الملفات موجودة:
   - lib/features/testing/local_db_test_screen.dart
   - lib/features/testing/invoices_test_screen.dart
   - lib/features/testing/sync_queue_test_screen.dart

✓ تحقق: أن screen_registry.dart مُحدّث بالحالات الجديدة
```

### **المشكلة: قاعدة البيانات فارغة**

```
✓ حل: اضغط أزرار الاختبار بالترتيب
   1. LocalDatabase → ➕ إضافة منتج
   2. LocalDatabase → 💰 إنشاء فاتورة
   3. Invoices → 💰 إنشاء فواتير
```

---

## 📊 الإحصائيات

```
الشاشات المضافة:   3 شاشات
الاختبارات:        16 اختبار
الأيقونات المُستخدمة: مختلفة
الألوان:           ملونة وسهلة التمييز
```

---

## ✨ الملاحظات المهمة

1. **شاشات الاختبار موجودة في المشروع الفعلي**
   - لا تحتاج خادم خارجي
   - تعمل مع البيانات المحلية مباشرة

2. **جميع البيانات محلية**
   - لا ترسل للخادم
   - آمنة للاختبار والتطوير

3. **يمكن تشغيل الاختبارات بأي ترتيب**
   - كل اختبار مستقل عن الآخر
   - البيانات محفوظة بين الاختبارات

4. **سهولة الإضافة للقائمة**
   - شاشات جديدة تُضاف تلقائياً
   - مرئية في القائمة الجانبية

---

## 🎯 الخطوة التالية

بعد اختبار جميع الشاشات بنجاح:

```
✅ اليوم 1-2: LocalDatabase ← مكتمل ✓
📋 اليوم 3-4: ConnectivityService ← التالي
🌐 اليوم 5: Backend Integration
🧪 اليوم 6-7: Testing & Refinement
```

---

**تاريخ الإنشاء:** 2026-09-15  
**الحالة:** ✅ جاهز للاختبار

---

## 🚀 ابدأ الآن!

```bash
# 1. تأكد أن التطبيق مشغّل
flutter run

# 2. سجّل الدخول

# 3. افتح القائمة الجانبية

# 4. اختر "اختبار" من القائمة

# 5. ابدأ الاختبارات!
```

**استمتع بالاختبار!** 🎉
