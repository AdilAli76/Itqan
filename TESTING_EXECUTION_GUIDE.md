# 🧪 **دليل الاختبار العملي - Phase 5**

**المرحلة**: الاختبار الشامل  
**الموعد**: الآن  
**الهدف**: ضمان جودة 100% قبل الإطلاق

---

## ✅ **1. فحص البناء (Build Check)**

### **الخطوة الأولى: تأكد من عدم وجود أخطاء بناء**

```bash
# في Terminal:
flutter clean
flutter pub get
flutter analyze

# ماذا تتوقع:
✅ لا توجد أخطاء (Errors)
✅ لا توجد تحذيرات حرجة (Critical Warnings)
✅ النتيجة: "No issues found"
```

### **قائمة التحقق:**
```
[ ] لا توجد أخطاء بناء
[ ] لا توجد تحذيرات حرجة
[ ] flutter analyze يعطي نتيجة خضراء
[ ] جميع الحزم محدثة
```

---

## 🌐 **2. اختبار الويب (Web Testing)**

### **الخطوة الأولى: شغّل التطبيق على الويب**

```bash
flutter run -d chrome
```

### **اختبار كل شاشة:**

#### **Invoices Screen:**
```
[ ] LoadingTest (التحميل):
    ✅ تظهر رسالة "جاري التحميل"
    ✅ الأرقام تحدّث بسرعة
    ✅ لا تتجمد الواجهة

[ ] SearchTest (البحث):
    ✅ اكتب كلمة في حقل البحث
    ✅ النتائج تتحدث (debounce يعمل)
    ✅ مسح البحث يعرّض كل البيانات

[ ] FilterTest (الفلاتر):
    ✅ اختر "معلقة" من القائمة
    ✅ الجدول يُحدّث فوراً
    ✅ الترقيم يعاد للصفحة 1

[ ] ButtonsTest (الأزرار):
    ✅ زر "تحديث" يعيد تحميل البيانات
    ✅ زر "طباعة" يظهر رسالة
    ✅ زر "تصدير" يظهر رسالة
    ✅ زر "فاتورة جديدة" يعمل

[ ] TableTest (الجدول):
    ✅ الصفوف تعرض البيانات بشكل صحيح
    ✅ الألوان والخطوط واضحة
    ✅ التمرير الأفقي يعمل (إن لزم)
    ✅ الترقيم يعمل بشكل صحيح

[ ] PaginationTest (الترقيم):
    ✅ الانتقال للصفحة التالية يعمل
    ✅ الانتقال للصفحة السابقة يعمل
    ✅ العدد الكلي يظهر بشكل صحيح
```

#### **Dashboard Screen:**
```
[ ] StatsCardTest (بطاقات الإحصائيات):
    ✅ الأرقام تظهر بشكل صحيح
    ✅ الـ Gradient جميل وملفت
    ✅ النسب المئوية تظهر مع الألوان الصحيحة
    ✅ الأيقونات واضحة

[ ] LayoutTest (التخطيط):
    ✅ على Desktop: 4 بطاقات في صف واحد
    ✅ على Tablet: بطاقتين في صف واحد
    ✅ على Mobile: بطاقة واحدة في صف
    ✅ المسافات بين البطاقات متساوية
```

#### **جميع الشاشات الأخرى:**
```
[ ] Customers / Inventory / Expenses / Users:
    ✅ ListToolbar يظهر بشكل صحيح
    ✅ البحث يعمل
    ✅ الفلاتر تعمل
    ✅ الجدول يعرض البيانات
    ✅ الأزرار تعمل
```

---

## 📱 **3. اختبار الجوال (Mobile Testing)**

### **الخطوة الأولى: شغّل على محاكي Android**

```bash
flutter emulators --launch Pixel_5_API_30
flutter run -d emulator-5000
```

### **الاختبار:**
```
[ ] ResponsivenessTest (التجاوب):
    ✅ ListToolbar يظهر بشكل مضغوط
    ✅ الأزرار صغيرة لكن قابلة للنقر
    ✅ الجدول يتمرر بسلاسة
    ✅ لا توجد عناصر مقطوعة

[ ] TouchTest (اللمس):
    ✅ الأزرار نقر بسهولة
    ✅ حقل البحث يتركيز بسهولة
    ✅ القوائم تفتح بسهولة
    ✅ التمرير سلس

[ ] PerformanceTest (الأداء):
    ✅ التطبيق لا يتجمد عند النقر
    ✅ البحث سريع (< 300ms)
    ✅ التمرير سلس (60 FPS)
    ✅ عدم وجود تأخير ملحوظ
```

---

## 🌙 **4. اختبار الوضع الليلي (Dark Mode)**

### **التفعيل:**
```
في الإعدادات → النمط → اختر "داكن"
أو في النظام → النمط → داكن
```

### **الاختبار:**
```
[ ] ColorsTest (الألوان):
    ✅ الخلفيات داكنة وليست سوداء تماماً
    ✅ النصوص بيضاء وواضحة
    ✅ الأيقونات مرئية
    ✅ الحدود واضحة

[ ] ContrastTest (التباين):
    ✅ النصوص واضحة بسهولة
    ✅ الأزرار مرئية بوضوح
    ✅ لا توجد نصوص على خلفيات داكنة جداً

[ ] TransitionTest (الانتقال):
    ✅ الانتقال من الفاتح للداكن سلس
    ✅ بدون وميض أو تأخير
    ✅ الألوان تتغير بسلاسة
```

---

## ⚡ **5. اختبار الأداء (Performance)**

### **أدوات القياس:**

#### **Chrome DevTools:**
```
1. افتح Chrome DevTools (F12)
2. اذهب إلى Performance tab
3. اضغط Record
4. افعل الأنشطة (بحث، فلتر، تمرير)
5. اضغط Stop
6. احلل النتائج
```

#### **ماذا تبحث عنه:**
```
✅ FPS:
   - القيمة: 60 FPS دائماً
   - إذا انخفضت: هناك مشكلة أداء

✅ Main Thread:
   - القيمة: < 16ms لكل frame
   - إذا زادت: يوجد blocking work

✅ Memory:
   - لا ترتفع بسرعة
   - لا تصل لـ 500MB

✅ Network:
   - الطلبات سريعة
   - بدون وقت انتظار طويل
```

### **قائمة الاختبار:**
```
[ ] SearchPerformance (سرعة البحث):
    ✅ الرد الأول: < 300ms
    ✅ التحديث: < 500ms
    ✅ لا توجد تأخيرات واضحة

[ ] FilterPerformance (سرعة الفلاتر):
    ✅ التطبيق الفوري: < 200ms
    ✅ سلاسة الانتقال

[ ] ScrollPerformance (سلاسة التمرير):
    ✅ FPS ثابتة على 60
    ✅ بدون جاجز أو توقفات

[ ] LoadPerformance (سرعة التحميل):
    ✅ الصفحة الأولى: < 2 ثانية
    ✅ الشاشات الأخرى: < 1 ثانية
```

---

## 🔒 **6. اختبار الأمان (Security)**

### **الاختبار:**
```
[ ] DataProtection (حماية البيانات):
    ✅ بيانات المستخدم مشفرة
    ✅ لا توجد بيانات حساسة في Logs
    ✅ الـ API calls مشفرة (HTTPS)

[ ] InputValidation (التحقق من المدخلات):
    ✅ جرّب إدخال أحرف غريبة
    ✅ لا توجد أخطاء
    ✅ التطبيق يتعامل معها بحكمة

[ ] SessionSecurity (أمان الجلسة):
    ✅ تسجيل الخروج يعمل
    ✅ انتهاء الجلسة يعمل
    ✅ بدون تسرب البيانات

[ ] NetworkSecurity (أمان الشبكة):
    ✅ لا توجد بيانات في Plain Text
    ✅ جميع الطلبات HTTPS
    ✅ لا توجد Hardcoded credentials
```

---

## ♿ **7. اختبار الوصولية (Accessibility)**

### **الاختبار:**
```
[ ] ScreenReaderTest (قارئ الشاشة):
    ✅ الأسماء واضحة
    ✅ الأزرار قابلة للتفعيل
    ✅ الحقول لها Labels واضحة

[ ] KeyboardNavigationTest (الملاحة بـ Keyboard):
    ✅ يمكن التنقل بـ Tab
    ✅ يمكن النقر بـ Enter
    ✅ Focus واضح ومرئي

[ ] ContrastTest (نسبة التباين):
    ✅ النسبة 4.5:1 على الأقل
    ✅ النصوص واضحة على الخلفيات
```

---

## 👥 **8. اختبار سهولة الاستخدام (Usability)**

### **الاختبار مع المستخدم:**
```
[ ] FirstImpressionTest (الانطباع الأول):
    ✅ يفهم الواجهة بسهولة
    ✅ يجد الزر الأخضر "جديد"
    ✅ يعرف كيفية البحث

[ ] SearchTest (البحث):
    ✅ يكتب في حقل البحث
    ✅ ينقر على مسح البحث
    ✅ يتفهم نتائج البحث

[ ] FilterTest (الفلاتر):
    ✅ يجد قائمة الفلاتر
    ✅ يفهم الخيارات
    ✅ يرى النتائج تتحدث

[ ] FeedbackTest (الملاحظات):
    ✅ ما يعجبه؟
    ✅ ما لا يعجبه؟
    ✅ ما الذي يحتاج تحسين؟
```

---

## 📋 **9. قائمة الاختبار النهائية**

### **قبل الإطلاق (48 ساعة):**
```
[ ] جميع اختبارات الويب ✅
[ ] جميع اختبارات الجوال ✅
[ ] اختبار الوضع الليلي ✅
[ ] اختبار الأداء ✅
[ ] اختبار الأمان ✅
[ ] اختبار الوصولية ✅
[ ] اختبار سهولة الاستخدام ✅
[ ] لا توجد أخطاء حرجة ✅
[ ] الأداء مقبول ✅
[ ] موافقة المستخدمين ✅
```

---

## 🐛 **10. معالجة الأخطاء**

### **عند إيجاد خطأ:**

```
1. سجّل الخطأ:
   - اسم الخطأ
   - خطوات إعادة الإنتاج
   - متى يحدث
   - صورة أو فيديو

2. صنّف الخطأ:
   🔴 حرج (Critical): يعطل التطبيق
   🟠 عالي (High): يمنع الاستخدام
   🟡 متوسط (Medium): يؤثر على التجربة
   🟢 منخفض (Low): تحسين بسيط

3. أصلح الخطأ:
   - تعديل الكود
   - اختبار من جديد
   - تأكد من الإصلاح
```

---

## 📊 **11. تقرير الاختبار**

### **بعد الانتهاء من الاختبار:**

```markdown
# تقرير الاختبار - [التاريخ]

## الملخص
- عدد الشاشات المختبرة: 8
- عدد الأخطاء المكتشفة: X
- عدد الأخطاء المصححة: Y
- النتيجة النهائية: ✅/❌

## الأخطاء المكتشفة
1. [الخطأ]: الحل: ✅/⏳

## الاختبارات الناجحة
- ✅ اختبار الويب
- ✅ اختبار الجوال
- ✅ اختبار الأداء
- إلخ...

## التوصيات
- ...

## الموافقة
- الحالة: ✅ جاهز للإطلاق
```

---

## ✅ **البدء الآن!**

### **الخطوة الأولى:**
```bash
flutter clean
flutter pub get
flutter analyze
```

### **ثم:**
```bash
flutter run -d chrome  # للويب
# أو
flutter run           # لأول جهاز متاح
```

### **ابدأ الاختبار الفعلي!**

---

**الحالة**: اختبار جاري 🧪  
**الهدف**: ضمان جودة 100%  
**الموعد**: هذا الأسبوع  

